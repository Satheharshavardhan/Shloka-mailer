# Deprecated / archive

Not used by the daily mailer. Kept for reference only.

| Folder | Contents |
|--------|----------|
| `scripts/` | Old PDF extraction scripts |
| `data/` | Old JSON outputs, raw scrape backup, source PDF |
| `logs/` | Scrape run logs |
| `output/` | Local HTML email previews (`--dry-run`) |

The live pipeline uses `structured_shlokas.json` at the project root and `main.py`.
